§6.8 B-J1>=1 Xdesc proven green: LOW = take J1 (Br N') descending

- The descending of LOW = P(seg M a (j0N-1)) is now a verified `have Xdesc`
  block (Finished PSS, 53s).  N-side m_6_2_P_additive at the same cut j0N
  (leftmin in N coords directly, no agree needed) gives the prefix split of
  Br N', and period agreement (segMN via nth_equalityI + agree) bridges to the
  M-side LOW; descending_take[OF descN'] closes.
- Required hoisting `idx` from inside segLOW's proof to the outer scope so both
  segLOW (M-side) and segLOW_N (N-side) can use it.
- Remaining for B-J1>=1: junc_cdom (descending_append junction) — last LOW =
  Br N' ! (J1-1), Br N' ! J1 = first component of P(seg N j0N (Lng N-1)) whose
  head equals ?Y!0's head, cdom via descN' + head-only dependence of cdom.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
