§6.8 B-J1>=1 CRUX proven green: j0N is a left-minimal row-0 P-cut (caseB)

- The genuinely-new part of sub-case B-J1>=1 is now a verified `have` block
  (Finished PSS, 52s): the cut j0N's row-0 parent p exists
  (m_5_1_ancestor_basic_1 -> entry N 0 j0' < entry N 0 j0N -> m_5_1_parent_exists_1),
  its N'-coordinate image p-j0' = parent N' 0 (j0N-j0') (adm_nextrel0_seg + the1_equality,
  with the inner parent stashed in a fresh b so parent_def does not double-unfold)
  is <= TrMax(N') by caseB, hence p <= j0'+TrMax = a-1 < a; the nextrel0 valley
  clause then gives `leftmin: entry N 0 j0N <= entry N 0 q` for all q in [a, j0N).
- `show ?thesis sorry` remains for the assembly (M-/N-side m_6_2_P_additive split
  at j0N + descending_append, mirroring sub-case A). 3 real sorries unchanged in
  count; B-J1>=1's crux de-risked.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
